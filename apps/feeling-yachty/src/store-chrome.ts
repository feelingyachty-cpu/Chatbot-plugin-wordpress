/** Auto-extracted chrome/bootstrap templates from staging bundle (module 405). */
import type { Colors } from './theme';

export function chromeCss(colors: Colors): string {
  return `\n  header.elementor-location-header,\n  footer.elementor-location-footer,\n  #fy-mobile-contact-bar,\n  .skip-link,\n  #trp_ald_modal_container,\n  .qodef-back-to-top,\n  #chat-widget-container,\n  chat-widget,\n  .woocommerce-breadcrumb,\n  .wc-block-breadcrumbs,\n  .related.products,\n  .up-sells,\n  .product_meta,\n  /* Native yacht screen already shows photos. Do not hide .fy-hero itself \u2014\n     on some templates it wraps the booking form and that left a blank sheet.\n     :has(form) / :has(#booking_date) keeps any carousel that actually contains\n     the charter widget. The 2 Sundancer extras step uses an Elementor / Swiper\n     gallery (hero + thumbs + "1 / 31"), not Woo's default product gallery. */\n  .woocommerce-product-gallery,\n  .woocommerce-product-gallery__wrapper,\n  .woocommerce-product-gallery__trigger,\n  div.product > div.images,\n  .wp-block-woocommerce-product-image-gallery,\n  .elementor-widget-woocommerce-product-images,\n  .fy-product-gallery,\n  .jet-woo-product-gallery,\n  .elementor-widget-image-carousel:not(:has(form)):not(:has(#booking_date)):not(:has([name="booking_date"])),\n  .elementor-widget-image-gallery:not(:has(form)):not(:has(#booking_date)):not(:has([name="booking_date"])),\n  .elementor-widget-n-carousel:not(:has(form)):not(:has(#booking_date)):not(:has([name="booking_date"])),\n  .elementor-widget-media-carousel:not(:has(form)):not(:has(#booking_date)):not(:has([name="booking_date"])),\n  .elementor-widget-loop-carousel:not(:has(form)):not(:has(#booking_date)):not(:has([name="booking_date"])),\n  .elementor-widget-gallery:not(:has(form)):not(:has(#booking_date)):not(:has([name="booking_date"])),\n  .e-n-carousel:not(:has(form)):not(:has(#booking_date)):not(:has([name="booking_date"])),\n  [data-widget_type="woocommerce-product-images.default"],\n  [data-widget_type="image-carousel.default"]:not(:has(form)):not(:has(#booking_date)),\n  [data-widget_type="nested-carousel.default"]:not(:has(form)):not(:has(#booking_date)),\n  [data-widget_type="media-carousel.default"]:not(:has(form)):not(:has(#booking_date)),\n  [data-widget_type="gallery.default"]:not(:has(form)):not(:has(#booking_date)),\n  /* Empty-cart perk row (Captain & crew / Fuel / Free to reschedule). */\n  .fy-cart-perks,\n  .fy-empty-perks,\n  .fy-empty-cart-perks,\n  .fy-app-empty-benefits,\n  .fy-cart-benefits,\n  .fy-cart-trust,\n  .fy-empty-trust,\n  .fy-bookbar,\n  .woocommerce-sticky-add-to-cart,\n  .sticky-add-to-cart {\n    display: none !important;\n  }\n\n  html, body {\n    background: ${colors.paper} !important;\n    -webkit-text-size-adjust: 100% !important;\n  }\n\n  main#content, .site-main {\n    padding: 0 !important;\n    margin: 0 !important;\n    max-width: 100% !important;\n  }\n\n  .wp-block-woocommerce-checkout,\n  .woocommerce-checkout,\n  .woocommerce-cart-form,\n  div.product {\n    margin: 0 !important;\n    padding: 10px 14px 28px !important;\n    max-width: 760px !important;\n  }\n\n  h1, h2, h3, .wc-block-components-title, .product_title {\n    color: ${colors.ink} !important;\n    line-height: 1.3 !important;\n  }\n  .product_title { font-size: 20px !important; }\n  h2, h3 { font-size: 17px !important; }\n\n  .wc-block-components-checkout-step__description, small, .description {\n    color: ${colors.muted} !important;\n  }\n\n  /* 16px keeps iOS from zooming the page when a field is focused. */\n  input[type="text"], input[type="email"], input[type="tel"], input[type="date"],\n  input[type="number"], input[type="password"], select, textarea,\n  .wc-block-components-text-input input {\n    font-size: 16px !important;\n    min-height: 48px !important;\n    border-radius: 12px !important;\n    border: 1px solid ${colors.line} !important;\n    background: ${colors.white} !important;\n    color: ${colors.ink} !important;\n  }\n\n  .wc-block-components-button,\n  .wc-block-components-checkout-place-order-button,\n  button.wc-block-components-button,\n  #place_order,\n  .checkout-button,\n  .single_add_to_cart_button {\n    background: ${colors.pink} !important;\n    border-color: ${colors.pink} !important;\n    color: ${colors.white} !important;\n    border-radius: 14px !important;\n    min-height: 54px !important;\n    font-weight: 800 !important;\n    font-size: 16px !important;\n    box-shadow: none !important;\n    width: 100% !important;\n  }\n  .single_add_to_cart_button.disabled { opacity: .45 !important; }\n\n  .wc-block-components-sidebar,\n  .wc-block-components-order-summary,\n  .wc-block-components-panel,\n  .fy-addon {\n    background: ${colors.card} !important;\n    border-radius: 14px !important;\n    border: 1px solid ${colors.line} !important;\n  }\n\n  /* WooCommerce Blocks collapses the order summary to display:none at mobile\n     widths, and this WebView is always mobile-width \u2014 so the charter duration,\n     start time, booking date and guest count were invisible at the moment of\n     payment. Forcing it open is the difference between "Order summary $875.00"\n     and the guest actually seeing what they are paying for. */\n  .wc-block-components-checkout-order-summary__content {\n    display: block !important;\n  }\n\n  .wc-block-components-product-details__name {\n    color: ${colors.muted} !important;\n  }\n  .wc-block-components-product-details__value {\n    color: ${colors.ink} !important;\n    font-weight: 700 !important;\n  }\n\n  .elementor-section-boxed > .elementor-container,\n  .e-con-inner { max-width: 100% !important; }\n  `;
}

export function accountChromeCss(colors: Colors): string {
  return `\n  header.elementor-location-header,\n  footer.elementor-location-footer,\n  #fy-mobile-contact-bar,\n  .skip-link,\n  #trp_ald_modal_container,\n  .qodef-back-to-top,\n  #chat-widget-container,\n  chat-widget,\n  .woocommerce-breadcrumb,\n  .wc-block-breadcrumbs,\n  .elementor-page-title,\n  .page-title,\n  /* Account sidebar: the app's tabs already cover these, and its Logout would\n     leave the app signed in while the website signed out. */\n  .woocommerce-MyAccount-navigation {\n    display: none !important;\n  }\n\n  html, body {\n    background: ${colors.paper} !important;\n    -webkit-text-size-adjust: 100% !important;\n  }\n\n  main#content, .site-main {\n    padding: 0 !important;\n    margin: 0 !important;\n    max-width: 100% !important;\n  }\n\n  .woocommerce-MyAccount-content,\n  .woocommerce-account .woocommerce,\n  .woocommerce-form-login,\n  .woocommerce-form-register {\n    width: 100% !important;\n    max-width: 640px !important;\n    float: none !important;\n    margin: 0 auto !important;\n    padding: 12px 14px 32px !important;\n  }\n\n  h1, h2, h3, legend {\n    color: ${colors.ink} !important;\n    line-height: 1.3 !important;\n  }\n\n  label, .woocommerce-form__label, p {\n    color: ${colors.ink} !important;\n  }\n\n  /* Inputs sized for a thumb rather than a desktop pointer. */\n  input[type=text], input[type=email], input[type=password], input[type=tel],\n  select, textarea {\n    background: ${colors.white} !important;\n    color: ${colors.ink} !important;\n    border: 1px solid ${colors.line} !important;\n    border-radius: 12px !important;\n    min-height: 48px !important;\n    font-size: 16px !important; /* under 16px iOS zooms on focus */\n    padding: 10px 12px !important;\n    width: 100% !important;\n    box-sizing: border-box !important;\n  }\n\n  button, .button, input[type=submit] {\n    background: ${colors.pink} !important;\n    color: ${colors.white} !important;\n    border: 0 !important;\n    border-radius: 999px !important;\n    min-height: 48px !important;\n    font-weight: 800 !important;\n    font-size: 16px !important;\n    width: 100% !important;\n    padding: 12px 18px !important;\n  }\n\n  .woocommerce-message, .woocommerce-error, .woocommerce-info {\n    border-radius: 12px !important;\n    background: ${colors.card} !important;\n    color: ${colors.ink} !important;\n    border-left: 4px solid ${colors.pink} !important;\n  }\n  `;
}

export function accountBootstrap(css: string): string {
  return `(function(){\n  var ID = 'fy-app-account-chrome';\n  var CSS = ${JSON.stringify(css)};\n  function applyCss(){\n    if (document.getElementById(ID)) { return; }\n    var head = document.head || document.documentElement;\n    if (!head) { return; }\n    var s = document.createElement('style');\n    s.id = ID; s.type = 'text/css';\n    s.appendChild(document.createTextNode(CSS));\n    head.appendChild(s);\n  }\n  applyCss();\n  document.addEventListener('DOMContentLoaded', applyCss);\n  // WooCommerce swaps parts of the account pages in via AJAX.\n  var tries = 0;\n  var iv = setInterval(function(){ applyCss(); if (++tries > 20) { clearInterval(iv); } }, 250);\n  true;\n})();`;
}

export function bookingBootstrap(css: string, prefill: Record<string, unknown>): string {
  return `(function(){\n  var ID = 'fy-app-chrome';\n  var CSS = ${JSON.stringify(css)};\n  var P = ${JSON.stringify(prefill)};\n\n  function applyCss(){\n    if (document.getElementById(ID)) { return; }\n    var head = document.head || document.documentElement;\n    if (!head) { return; }\n    var s = document.createElement('style');\n    s.id = ID; s.type = 'text/css';\n    s.appendChild(document.createTextNode(CSS));\n    head.appendChild(s);\n  }\n\n  \n  var GALLERY_SEL = [\n    '.woocommerce-product-gallery',\n    '.woocommerce-product-gallery__wrapper',\n    '.woocommerce-product-gallery__trigger',\n    '.wp-block-woocommerce-product-image-gallery',\n    '.elementor-widget-woocommerce-product-images',\n    '.elementor-widget-image-carousel',\n    '.elementor-widget-image-gallery',\n    '.elementor-widget-n-carousel',\n    '.elementor-widget-media-carousel',\n    '.elementor-widget-loop-carousel',\n    '.elementor-widget-gallery',\n    '.fy-product-gallery',\n    '.jet-woo-product-gallery',\n    '.e-n-carousel',\n    '[data-widget_type="woocommerce-product-images.default"]',\n    '[data-widget_type="image-carousel.default"]',\n    '[data-widget_type="nested-carousel.default"]',\n    '[data-widget_type="media-carousel.default"]',\n    '[data-widget_type="gallery.default"]'\n  ].join(',');\n  var GALLERY_MATCH = GALLERY_SEL + ', .elementor-widget-image, [data-widget_type="image.default"]';\n\n  function fyWantNoGallery(){\n    try {\n      if (/[?&]fy_nogallery=1(?:&|$)/.test(String(location.search || ''))) { return true; }\n    } catch (e) {}\n    return !!(document.body && document.body.classList.contains('single-product'));\n  }\n  function fyBookingNode(root){\n    root = root || document;\n    try {\n      return root.querySelector(\n        '#booking_date, input[name="booking_date"], select[name="charter_start_time"], form.cart, form.variations_form, button.single_add_to_cart_button'\n      );\n    } catch (e) { return null; }\n  }\n  function fyHasBooking(el){\n    if (!el || !el.querySelector) { return false; }\n    return !!fyBookingNode(el);\n  }\n  function fyIsGalleryWidget(el){\n    try { return !!(el && el.matches && el.matches(GALLERY_MATCH)); } catch (e) { return false; }\n  }\n  function fyLooksLikeGallery(el){\n    if (!el || el.nodeType !== 1 || fyHasBooking(el)) { return false; }\n    if (fyIsGalleryWidget(el)) { return true; }\n    try {\n      if (el.querySelectorAll('img').length >= 3) { return true; }\n      if (el.querySelector('.swiper, .swiper-wrapper, .swiper-container, .flex-control-thumbs, .flex-viewport')) { return true; }\n    } catch (e) {}\n    return false;\n  }\n  function fyStripMedia(el){\n    try {\n      var media = el.querySelectorAll('img, source, video');\n      for (var i = 0; i < media.length; i++) {\n        var m = media[i];\n        m.removeAttribute('src');\n        m.removeAttribute('srcset');\n        m.removeAttribute('sizes');\n        m.removeAttribute('data-src');\n        m.removeAttribute('data-srcset');\n        m.removeAttribute('data-large_image');\n        m.removeAttribute('data-lazy-src');\n        m.removeAttribute('data-lazy-srcset');\n        m.removeAttribute('data-bg');\n        try { m.src = ''; } catch (e1) {}\n        try { m.srcset = ''; } catch (e2) {}\n      }\n    } catch (e) {}\n  }\n  function fyNuke(el){\n    if (!el || el.nodeType !== 1) { return; }\n    if (el.id === 'fy-app-chrome' || el.id === 'fy-app-chrome-boot') { return; }\n    if (el.getAttribute && el.getAttribute('data-fy-nogallery') === '1') { return; }\n    try { el.setAttribute('data-fy-nogallery', '1'); } catch (e) {}\n    fyStripMedia(el);\n    try { el.style.setProperty('display', 'none', 'important'); } catch (e) {}\n    try { if (el.parentNode) { el.parentNode.removeChild(el); } } catch (e) {}\n  }\n  function fyHideTree(el){\n    if (!el || el.nodeType !== 1) { return; }\n    if (fyHasBooking(el)) {\n      var kids = el.children || [];\n      for (var i = 0; i < kids.length; i++) { fyHideTree(kids[i]); }\n      return;\n    }\n    if (fyLooksLikeGallery(el) || fyIsGalleryWidget(el)) { fyNuke(el); }\n  }\n  function fyStripGalleryImg(img){\n    if (!img || !img.closest) { return; }\n    try {\n      if (img.closest('form.cart, form.variations_form, .fy-addon, .woocommerce-variation-add-to-cart')) { return; }\n      var wrap = img.closest(GALLERY_SEL);\n      if (wrap && !fyHasBooking(wrap)) {\n        img.removeAttribute('src');\n        img.removeAttribute('srcset');\n        img.removeAttribute('data-src');\n        img.removeAttribute('data-srcset');\n        img.removeAttribute('data-large_image');\n        try { img.src = ''; } catch (e) {}\n      }\n    } catch (e) {}\n  }\n  function fyHideStoreGallery(){\n    if (!fyWantNoGallery() && !fyBookingNode(document)) { return; }\n    var found;\n    try { found = document.querySelectorAll(GALLERY_SEL); } catch (e) { found = []; }\n    var formReady = !!fyBookingNode(document);\n    for (var i = 0; i < found.length; i++) {\n      var el = found[i];\n      if (fyHasBooking(el)) { fyHideTree(el); continue; }\n      var maybeWrap = false;\n      try {\n        maybeWrap = el.matches(\n          '.elementor-widget-n-carousel, .elementor-widget-image-carousel, .elementor-widget-media-carousel, .elementor-widget-loop-carousel, .elementor-widget-gallery, .e-n-carousel, [data-widget_type="nested-carousel.default"], [data-widget_type="image-carousel.default"], [data-widget_type="media-carousel.default"]'\n        );\n      } catch (e) {}\n      // Nested carousels sometimes wrap the charter form. Wait until the\n      // form exists so we don't delete the widget the extras fields live in.\n      if (maybeWrap && !formReady) { continue; }\n      fyNuke(el);\n    }\n    var anchor = fyBookingNode(document);\n    if (!anchor) { return; }\n    var node = anchor;\n    for (var depth = 0; depth < 16 && node && node !== document.body && node !== document.documentElement; depth++) {\n      var sib = node.previousElementSibling;\n      while (sib) {\n        var prev = sib.previousElementSibling;\n        if (fyHasBooking(sib)) { fyHideTree(sib); }\n        else if (fyLooksLikeGallery(sib)) { fyNuke(sib); }\n        sib = prev;\n      }\n      node = node.parentElement;\n    }\n  }\n  \n  \n  function fyIsCartPage(){\n    try {\n      var path = String(location.pathname || '').toLowerCase();\n      var search = String(location.search || '').toLowerCase();\n      if (path.indexOf('order-received') !== -1 || search.indexOf('order-received') !== -1) { return false; }\n      if (path === '/cart' || path === '/cart/') { return true; }\n      return path.indexOf('/cart/') !== -1;\n    } catch (e) { return false; }\n  }\n  function fyHide(el){\n    if (!el || el.nodeType !== 1) { return; }\n    if (el.getAttribute && el.getAttribute('data-fy-hide-perks') === '1') { return; }\n    try { el.setAttribute('data-fy-hide-perks', '1'); } catch (e) {}\n    try { el.style.setProperty('display', 'none', 'important'); } catch (e) {}\n  }\n  function fyHideCartPerks(){\n    if (!fyIsCartPage()) { return; }\n    var needles = [\n      'captain & crew included',\n      'captain and crew included',\n      'fuel included',\n      'free to reschedule',\n      'capit\xe1n y tripulaci\xf3n incluidos',\n      'capitan y tripulacion incluidos',\n      'combustible incluido',\n      'reprogramaci\xf3n gratis',\n      'reprogramacion gratis'\n    ];\n    var hits = [];\n    var nodes;\n    try { nodes = document.querySelectorAll('li, p, span, div, small, strong, h6, a, figcaption'); }\n    catch (e) { return; }\n    for (var i = 0; i < nodes.length; i++) {\n      var el = nodes[i];\n      var raw = (el.innerText || el.textContent || '').replace(/\\s+/g, ' ').trim().toLowerCase();\n      if (!raw || raw.length > 72) { continue; }\n      for (var n = 0; n < needles.length; n++) {\n        if (raw === needles[n] || (raw.indexOf(needles[n]) !== -1 && raw.length <= needles[n].length + 8)) {\n          hits.push(el);\n          break;\n        }\n      }\n    }\n    if (!hits.length) { return; }\n    for (var h = 0; h < hits.length; h++) {\n      var item = hits[h];\n      try {\n        item = item.closest(\n          '.elementor-widget-icon-box, .elementor-icon-box-wrapper, .elementor-widget-icon-list, .elementor-icon-list-item, [data-widget_type="icon-box.default"], li, .fy-perk, .e-icon-box'\n        ) || item;\n      } catch (e) {}\n      fyHide(item);\n    }\n    // If the three items shared a wrapper that is now empty of other copy, hide it too.\n    var first = hits[0];\n    var wrap = first && first.parentElement;\n    for (var depth = 0; depth < 6 && wrap && wrap !== document.body; depth++) {\n      var txt = (wrap.innerText || wrap.textContent || '').replace(/\\s+/g, ' ').trim().toLowerCase();\n      if (!txt) { fyHide(wrap); break; }\n      if (/no charters booked|talk to our team|pick your yacht/.test(txt)) { break; }\n      var leftover = txt\n        .replace(/captain & crew included/g, '')\n        .replace(/captain and crew included/g, '')\n        .replace(/fuel included/g, '')\n        .replace(/free to reschedule/g, '')\n        .replace(/[\u2022|/\\-\u2013\u2014]/g, '')\n        .replace(/\\s+/g, ' ')\n        .trim();\n      if (!leftover) { fyHide(wrap); break; }\n      wrap = wrap.parentElement;\n    }\n  }\n  \n\n  function set(el, value){\n    if (!el || !value) { return false; }\n    if (el.value === value) { return false; }\n    el.value = value;\n    // jQuery binds with addEventListener, so native events reach Woo's handlers.\n    el.dispatchEvent(new Event('input', { bubbles: true }));\n    el.dispatchEvent(new Event('change', { bubbles: true }));\n    return true;\n  }\n\n  function find(id, extra){\n    return document.getElementById(id) || document.querySelector(extra);\n  }\n\n  function prefill(){\n    set(find('booking_date', 'input[name="booking_date"]'), P.date);\n    set(find('charter_start_time', 'select[name="charter_start_time"], input[name="charter_start_time"]'), P.time);\n    set(\n      find('pa_charter-duration', 'select[name="attribute_pa_charter-duration"], select[name="duration"]'),\n      P.duration\n    );\n    set(\n      find('pa_passenger-count', 'select[name="attribute_pa_passenger-count"], select[name="guests"], input[name="guests"]'),\n      P.guests ? String(P.guests) : ''\n    );\n  }\n\n  function forward(raw){\n    try {\n      var d = typeof raw === 'string' ? JSON.parse(raw) : raw;\n      if (!d || !d.fy) { return; }\n      if (d.fy === 'nogallery' || d.fy === 'boot') { fyHideStoreGallery(); applyCss(); return; }\n      if (window.ReactNativeWebView && window.ReactNativeWebView.postMessage) {\n        window.ReactNativeWebView.postMessage(JSON.stringify(d));\n      }\n    } catch (e) {}\n  }\n  window.addEventListener('message', function(e){ forward(e.data); });\n\n  function tick(){ applyCss(); fyHideStoreGallery(); fyHideCartPerks(); prefill(); }\n\n  tick();\n  if (document.readyState === 'loading') {\n    document.addEventListener('DOMContentLoaded', tick);\n  }\n  // The product form and the blocks checkout both mount late and re-render.\n  try {\n    new MutationObserver(function(muts){\n      for (var i = 0; i < muts.length; i++) {\n        var nodes = muts[i].addedNodes;\n        for (var j = 0; j < nodes.length; j++) {\n          var n = nodes[j];\n          if (n && n.nodeName === 'IMG') { fyStripGalleryImg(n); }\n          if (n && n.querySelectorAll) {\n            var imgs = n.querySelectorAll('img');\n            for (var k = 0; k < imgs.length; k++) { fyStripGalleryImg(imgs[k]); }\n          }\n        }\n      }\n      tick();\n    }).observe(document.documentElement, { childList: true, subtree: true });\n  } catch (e) {}\n  setTimeout(tick, 400);\n  setTimeout(tick, 1200);\n})();\ntrue;`;
}

